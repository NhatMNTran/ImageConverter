<Window x:Class="ImageConverterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Image Converter"
        Height="450"
        Width="600"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Choose File -->
        <StackPanel Grid.Row="0" Margin="0,10">
            <TextBlock Text="Choose File" FontWeight="Bold"/>
            <DockPanel>
                <TextBox x:Name="InputFileTextBox"
                         Width="400"
                         Margin="0,0,10,0"/>

                <Button Content="Browse"
                        Width="100"
                        Click="BrowseFile_Click"/>
            </DockPanel>
        </StackPanel>

        <!-- Current Format -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Current File Type" FontWeight="Bold"/>

            <ComboBox x:Name="CurrentFormatComboBox"
                      Width="200"/>
        </StackPanel>

        <!-- Convert To -->
        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Convert To" FontWeight="Bold"/>

            <ComboBox x:Name="TargetFormatComboBox"
                      Width="200"/>
        </StackPanel>

        <!-- Destination Folder -->
        <StackPanel Grid.Row="3" Margin="0,10">
            <TextBlock Text="Destination Folder" FontWeight="Bold"/>

            <DockPanel>
                <TextBox x:Name="DestinationTextBox"
                         Width="400"
                         Margin="0,0,10,0"/>

                <Button Content="Browse"
                        Width="100"
                        Click="BrowseDestination_Click"/>
            </DockPanel>
        </StackPanel>

        <!-- Convert Button -->
        <Button Grid.Row="4"
                Content="Convert Image"
                Height="50"
                Width="200"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="16"
                Click="ConvertButton_Click"/>

    </Grid>
</Window>
